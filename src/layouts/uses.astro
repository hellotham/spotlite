---
import Layout from './layout.astro'

interface Props {
  frontmatter: {
    title: string
    description: string
    list: {
      section: string
      items: {
        item: string
        description: string
      }[]
    }[]
  }
}

const { frontmatter } = Astro.props
---

<Layout title={frontmatter.title} description={frontmatter.description} itemPage={true}>
  <div class='mt-16 sm:mt-32 sm:px-8'>
    <div class='mx-auto max-w-7xl lg:px-8'>
      <div class='px-4 relative lg:px-12 sm:px-8'>
        <div class='mx-auto max-w-2xl lg:max-w-5xl'>
          <header class='max-w-2xl'>
            <h1
              class='text-4xl text-blackBeauty tracking-tight font-bold sm:text-5xl dark:text-sugarSwizzle'
            >
              {frontmatter.title}
            </h1>
            <p class='text-xl italic text-morningGlory mt-6'>
              {frontmatter.description}
            </p>
            <div class='mt-6 prose dark:prose-invert max-w-none'>
              <slot />
            </div>
          </header><div class='mt-16 sm:mt-20'>
            <div class='space-y-20'>
              {
                frontmatter.list.map((section) => (
                  <section
                    aria-labelledby=':R6l6:'
                    class='md:pl-6 md:border-l md:border-barelyPink md:dark:border-grapeade/40'
                  >
                    <div class='gap-y-8 grid grid-cols-1 max-w-3xl items-baseline md:grid-cols-4'>
                      <>
                        <h2
                          id=':R6l6:'
                          class='text-sm text-blackBeauty font-semibold dark:text-sugarSwizzle'
                        >
                          {section.section}
                        </h2>
                        <div class='md:col-span-3'>
                          <ul class='space-y-16'>
                            {section.items.map((item) => (
                              <li class='group flex flex-col items-start relative'>
                                <h3 class='text-base text-blackBeauty tracking-tight font-semibold dark:text-sugarSwizzle'>
                                  {item.item}
                                </h3>
                                <p class='text-sm text-graniteGray mt-2 relative z-10 dark:text-opalGray'>
                                  {item.description}
                                </p>
                              </li>
                            ))}
                          </ul>
                        </div>
                      </>
                    </div>
                  </section>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
