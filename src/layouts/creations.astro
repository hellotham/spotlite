---
import Layout from '../layouts/layout.astro'

interface Props {
  frontmatter: {
    title: string
    description: string
    list: {
      section: string
      items: {
        item: string
        description: string
        parent: string
        action: string
        link: string
      }[]
    }[]
  }
}

const { frontmatter } = Astro.props
---

<Layout title={frontmatter.title} description={frontmatter.description}>
  <div class='mt-16 sm:mt-32 sm:px-8'>
    <div class='mx-auto max-w-7xl lg:px-8'>
      <div class='px-4 relative lg:px-12 sm:px-8'>
        <div class='mx-auto max-w-2xl lg:max-w-5xl'>
          <header class='max-w-2xl'>
            <h1
              class='text-4xl text-pink-800 tracking-tight font-bold sm:text-5xl dark:text-pink-100'
            >
              {frontmatter.description}
            </h1><div class='text-base text-zinc-600 mt-6 dark:text-zinc-400'>
              <slot />
            </div>
          </header><div class='mt-16 sm:mt-20'>
            <div class='space-y-20'>
              {
                frontmatter.list.map((section) => (
                  <section
                    aria-labelledby=':R6l6:'
                    class='md:pl-6 md:border-l md:border-pink-100 md:dark:border-pink-700/40'
                  >
                    <div class='gap-y-8 grid grid-cols-1 max-w-3xl items-baseline md:grid-cols-4'>
                      <h2
                        id=':R6l6:'
                        class='text-sm text-zinc-800 font-semibold dark:text-zinc-100'
                      >
                        {section.section}
                      </h2>
                      <div class='md:col-span-3'>
                        <div class='space-y-16'>
                          {section.items.map((item) => (
                            <article class='group flex flex-col items-start relative'>
                              <h3 class='text-base text-zinc-800 tracking-tight font-semibold dark:text-zinc-100'>
                                <div class='bg-pink-50 opacity-0 scale-95 transition absolute z-0 sm:rounded-2xl dark:bg-pink-800/50 group-hover:opacity-100 group-hover:scale-100 -inset-x-4 -inset-y-6 sm:-inset-x-6' />
                                <a href={item.link}>
                                  <span class='absolute z-20 sm:rounded-2xl -inset-x-4 -inset-y-6 sm:-inset-x-6' />
                                  <span class='relative z-10'>{item.item}</span>
                                </a>
                              </h3>
                              <p class='text-sm text-zinc-400 mb-3 pl-3.5 flex items-center order-first relative z-10 dark:text-zinc-500'>
                                <span
                                  class='flex items-center inset-y-0 left-0 absolute'
                                  aria-hidden='true'
                                >
                                  <span class='rounded-full bg-pink-200 h-4 w-0.5 dark:bg-pink-500' />
                                </span>
                                {item.parent}
                              </p>
                              <p class='text-sm text-zinc-600 mt-2 relative z-10 dark:text-zinc-400'>
                                {item.description}
                              </p>
                              <div
                                aria-hidden='true'
                                class='text-sm text-purple-500 font-medium mt-4 flex items-center relative z-10'
                              >
                                {item.action}
                                <span class='i-heroicons-chevron-right ml-1 h-4 w-4 stroke-current' />
                              </div>
                            </article>
                          ))}
                        </div>
                      </div>
                    </div>
                  </section>
                ))
              }
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</Layout>
