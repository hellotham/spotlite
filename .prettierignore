# Ignore artifacts:
dist
.astro
coverage

# Vendored HTML kept verbatim. It is 1990s markup with unterminated tags that
# Prettier cannot parse, and reformatting it would defeat the point of keeping it.
public/rubato
public/RUBATO.ZIP

# Standard
**/.git
**/.svn
**/.hg
**/node_modules

# lockfiles
pnpm-lock.yaml
yarn.lock
package-lock.json
